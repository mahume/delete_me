import button from '../locales/en/button.json';
import copy from '../locales/en/copy.json';
import main from '../locales/en/main.json';

const resources = {
  button,
  copy,
  main
} as const;

export default resources;
